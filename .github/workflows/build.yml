name: Build

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Restore
        run: dotnet restore Jellyfin.Plugin.Link.sln

      - name: Build
        run: dotnet build Jellyfin.Plugin.Link.sln -c Release --no-restore

      - name: Package
        run: |
          mkdir -p artifacts/Link
          cp Jellyfin.Plugin.Link/bin/Release/net8.0/Jellyfin.Plugin.Link.dll artifacts/Link/
          cd artifacts
          zip -r link.zip Link

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: jellyfin-plugin-link
          path: artifacts/link.zip

  release:
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Restore
        run: dotnet restore Jellyfin.Plugin.Link.sln

      - name: Build
        run: dotnet build Jellyfin.Plugin.Link.sln -c Release --no-restore

      - name: Package
        run: |
          mkdir -p artifacts/Link
          cp Jellyfin.Plugin.Link/bin/Release/net8.0/Jellyfin.Plugin.Link.dll artifacts/Link/
          cd artifacts
          zip -r link.zip Link

      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/link.zip
          generate_release_notes: true
